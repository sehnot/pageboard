# Notice

The PDF files under this directory (and its subdirectories) are mirrored
from [py-pdf/sample-files](https://github.com/py-pdf/sample-files),
unmodified, preserving that repository's own numbered-folder layout.

They are licensed under **[CC-BY-SA-4.0](https://creativecommons.org/licenses/by-sa/4.0/)**
by the py-pdf project — a **different license** from the rest of this
repository (PageBoard's own code is MIT, see the root `LICENSE` file). This
directory exists purely as a local test-fixture corpus (see
`test/sample-files-compatibility.test.mjs` and the fixtures referenced from
`test/error-handling.test.mjs`, `test/e2e-critical-path.test.mjs`, and
`test/focus-mode.test.mjs`) and is not itself part of PageBoard's
distributed application code.

`pdf-files/error-cases/*.pdf` and `pdf-files/screenshot-files/*.pdf` are
unrelated to this notice — both are self-generated by this project (see
`CLAUDE.md`), not sourced from py-pdf/sample-files.
